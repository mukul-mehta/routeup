# Binaries (anchored to repo root so they don't shadow cmd/routeup/)
/bin/
/dist/
/routeup
/routeup.exe

# Test and coverage artifacts
*.test
*.out
coverage.*
profile.*

# Editor and OS
.DS_Store
.idea/
.vscode/
*.swp
*.swo

# Go workspace files (optional, kept out of repo by default)
go.work
go.work.sum

# Local environment
.env
.env.local
